{{ stubHeader .SourceFile "#" }}
extends Node
class_name {{ .ClassName }}Action

## Action node: {{ .ClassName }}
{{- if .Description }}
## {{ .Description }}
{{- end }}
{{- if .Parameters }}

# --- Parameters from spec ---
{{- range $key, $val := .Parameters }}
# {{ $key }} = {{ $val }}
{{- end }}
{{- end }}

func initialize(blackboard: Node) -> void:
	# TODO: Initialize action state
{{- if .Blackboard }}
	# Available blackboard variables:
{{- range .Blackboard }}
	#   blackboard.get("{{ .Name }}")  # {{ .Type }}{{ if .Description }} — {{ .Description }}{{ end }}
{{- end }}
{{- end }}
	pass

func tick(blackboard: Node, delta: float) -> int:
	# TODO: Implement action logic
	# Return 0 for Success, 1 for Failure, 2 for Running
	return 0

func terminate(blackboard: Node, status: int) -> void:
	# TODO: Cleanup when action ends
	pass
