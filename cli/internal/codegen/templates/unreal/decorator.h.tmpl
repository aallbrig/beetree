{{ stubHeader .SourceFile "//" }}
#pragma once

#include "CoreMinimal.h"
#include "BehaviorTree/BTDecorator.h"
#include "BTDecorator_{{ .ClassName }}.generated.h"

/**
 * Condition decorator: {{ .ClassName }}
{{- if .Description }}
 * {{ .Description }}
{{- end }}
{{- if .Parameters }}
 *
 * Parameters from spec:
{{- range $key, $val := .Parameters }}
 *   {{ $key }} = {{ $val }}
{{- end }}
{{- end }}
{{- if .Blackboard }}
 *
 * Blackboard variables:
{{- range .Blackboard }}
 *   {{ .Name }} ({{ .Type }}){{ if .Description }} — {{ .Description }}{{ end }}
{{- end }}
{{- end }}
 */
UCLASS()
class UBTDecorator_{{ .ClassName }} : public UBTDecorator
{
    GENERATED_BODY()

public:
    UBTDecorator_{{ .ClassName }}();

protected:
    virtual bool CalculateRawConditionValue(UBehaviorTreeComponent& OwnerComp, uint8* NodeMemory) const override;
};
